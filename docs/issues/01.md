# Validate companion-panel architecture

Implemented a persistent companion panel while the normal bar remains active.

- [x] Keep widgets registered through plugins[] after removing their top layout entries.
- [x] Receive the shell registry and shared theme context through panel injection.
- [x] Validate loading with the live duplicate guard, then mount Gmail once.
- [x] Verify unload/reload without duplicate instances and record tested versions.

See docs/design.md and docs/verification.md. Companion architecture selected.
